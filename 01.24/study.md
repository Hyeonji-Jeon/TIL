IA (사이트 맵) => Figma로 잡음

조회 / 처리 / 이동

웹 페이지
- 입력과 결과 가 분리되어있음
 : 회원가입
- 입력과 결과가 하나로 되어있음 
 : 게시판 리스트

URL 설계

사용자 시나리오
=> 메뉴조회 -> 메뉴 선택 -> 수량 조정 -> 장바구니 추가

request = in = read ( )
: 브라우저가 보낸 모든 정보를 읽음
=> getParameter( ) 로 쉽게 조회가능

response = out = write ( )
: 응답메세지를 구성

* : 다 받아들이겠다

PRG 패턴
: 웹 개발에서 자주 사용되는 디자인 패턴
: 중복 제출 방지
: URL에 데이터 노출 방지

조회 or 입력화면  => GET
처리 => POST

대상이 다른 목적지로 가도록 유도
=> Redirect

Redirect를 쓰는이유
: 도배 방지

POST -> F5 계속 누름 => 도배 됨
=> PRG 패턴 사용