TypeScript의 장점
- 타입이 엄격해서 에러메세지를 정확히 짚어줌
- 수정시 기존 코드 다 안뜯어고쳐도 됨
- 협업이 편

JS -> Ts 변경

1. <script> -> <script lang="ts">  :  ts 모드 ON
2. props에 타입 명시
3. $emit -> emits 타입 선언
4. ref(undefined) -> 명확한 타입


TypeScript 필수 문법 

- 타입지정
let 이름 : string = 'kim'
let 이름 : string[] = ['kim', 'park']
let 이름 : { name? : string } = { name : 'kim' }
let 이름 : string | number = 123

type Name = string | number  ---  type명은 보통 대문자로 나타냄 
let 이름 : Name = 123

function 함수(x :number) :number {
  return x * 2
}  ---  파라미터로 number, return 값으로 number

type Member = [number, boolean]  ---  무조건 첫 째는 number, 둘 째는 boolean
let john :Member = [123, true]

type Member = {
  [key :string] : string,  ---  글자로 된 모든 object 속성의 타입은 :string 임
}
let john : Member = { name : 'kim', age : '123' }

class User {
  name :string
  constructor(name :string) {
    this.name = name ;
  }
}
