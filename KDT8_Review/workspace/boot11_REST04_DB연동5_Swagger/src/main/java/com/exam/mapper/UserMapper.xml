<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.mapper.UserMapper">
   
   <select id="findAll" resultType="UserDTO">
       select id, username, birthdate
       from user
       order by id
   </select>
   
   <select id="findById" resultType="UserDTO" parameterType="long">
       select id, username, birthdate
       from user
       where id = #{id}
   </select>
	 
   <insert id="save" parameterType="UserDTO">
       insert into user ( id, username, birthdate )
       values (#{id}, #{username}, #{birthdate} )
   </insert> 
   
   <update id="update" parameterType="UserDTO">
      update user
      set username = #{username}, birthdate=#{birthdate}
      where id = #{id}
   </update>
   
   <delete id="delete" parameterType="long">
      delete from user
      where id = #{id}
   </delete>
	 
</mapper>

